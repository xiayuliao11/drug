server:
  port: 5001
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/
spring:
  application:
    name: order-feign
  main:
    allow-bean-definition-overriding: true

    thymeleaf:
      encoding: UTF-8
      servlet:
        content-type: text/html

